name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print message
        run: | 
          echo "Runner is working fine 
